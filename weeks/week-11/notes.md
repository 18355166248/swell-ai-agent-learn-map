# Week 11 笔记：LangGraph 零基础学习记录

> 当前状态：🟡 进行中（开始于 2026-06-01）

## 本周想回答的核心问题

- LangGraph 的 `StateGraph` 和手写 ReAct 循环在表达力上有什么差异？
- 框架版本代码量减少了多少？可读性变好还是变差？
- 用同一套评估任务集跑，通过率是否有差异？
- 什么场景下手写更好？什么场景下框架更好？
- LangGraph 的每个 API 在已有手写代码中对应什么？

## 统计口径（每次对比前先看这里）

手写版代码行数（`wc -l` 实测）：

| 文件                      | 行数    |
| ------------------------- | ------- |
| `agent/index.ts`          | 483     |
| `agent/tools/registry.ts` | 124     |
| `agent/memory.ts`         | 164     |
| **核心逻辑合计**          | **771** |
| `server.ts`               | 153     |
| **含服务层合计**          | **924** |

---

## 第 1 段：StateGraph 是什么

> `Annotation.Root` / `StateGraph` / `addNode` / `addEdge`

**关键理解**：

> 完成后补充。

**Aha 时刻**（哪个点让你突然理解了手写版和框架版的对应关系）：

> 完成后补充。

**遗留问题**：

> 完成后补充。

---

## 第 2 段：图怎么跑起来

> `compile()` / `invoke()` / `START` / `END`

**关键理解**：

> 完成后补充。

**Aha 时刻**：

> 完成后补充。

**遗留问题**：

> 完成后补充。

---

## 第 3 段：state 为什么自己累积

> `MessagesAnnotation` / Reducer

**关键理解**：

> 完成后补充。

**Aha 时刻**：

> 完成后补充。

**遗留问题**：

> 完成后补充。

---

## 第 4 段：节点里怎么调模型

> `ChatOpenAI` / `llm.invoke()`

**关键理解**：

> 完成后补充。

**Aha 时刻**：

> 完成后补充。

**遗留问题**：

> 完成后补充。

---

## 第 5 段：模型怎么决定调工具

> `.bindTools()` / `tool_calls` / `AIMessage`

**关键理解**：

> 完成后补充。

**Aha 时刻**：

> 完成后补充。

**遗留问题**：

> 完成后补充。

---

## 第 6 段：工具怎么被图执行

> `DynamicStructuredTool` / `ToolNode`

**关键理解**：

> 完成后补充。

**Aha 时刻**：

> 完成后补充。

**遗留问题**：

> 完成后补充。

---

## 第 7 段：图怎么进入循环

> `addConditionalEdges` / `shouldContinue` / `recursionLimit`

**关键理解**：

> 完成后补充。

**Aha 时刻**：

> 完成后补充。

**遗留问题**：

> 完成后补充。

---

## 第 8 段：运行时能力

> `stream()` / `MemorySaver` / `thread_id`

**关键理解**：

> 完成后补充。

**Aha 时刻**：

> 完成后补充。

**遗留问题**：

> 完成后补充。

---

## 最终对比结论

> 完成全部段落 + 评估对比后填写。

### 代码量对比

| 维度           | 手写版行数 | LangGraph 版行数 | 缩减比例 |
| -------------- | ---------- | ---------------- | -------- |
| Agent 核心循环 | 483        |                  |          |
| 工具注册       | 124        |                  |          |
| 对话记忆       | 164        |                  |          |
| HTTP/SSE 服务  | 153        |                  |          |

### 评估通过率对比

> 跑完同一套任务集后填写。

### 最终判断：什么时候手写，什么时候用框架？

> 这是整个 Week 11 最重要的结论。

---

## 对 Week 12 的判断

> 完成后补充。
>
> 预判：如果第 7 段的 ReAct 图已经稳定，Week 12 可以用 `interrupt()` 替换手写确认逻辑——这会是体验提升最明显的一步。
